machine:
  environment:
    LANG: en_US.UTF-8
    LC_CTYPE: en_US.UTF-8
dependencies:
  override:
    - exit 0
test:
  override:
    - bash -n SSBuild.sh
    - ./SSBuild.sh Example/Example.config
    - ls Example/output/release/SSBuildExample.ipa
general:
  artifacts:
    - output/release
